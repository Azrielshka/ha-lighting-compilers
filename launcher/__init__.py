# -*- coding: utf-8 -*-
"""Launcher: GUI-обёртка над CLI-скриптами проекта."""

# Версия, которую лаунчер показывает в шапке.
#
# Суффикс -dev намеренный: тег v3.0.0 ставится при мерже feat/table-v2 в main
# (см. docs/ROADMAP.md). Пока мержа нет — писать «3.0.0» значило бы врать
# наладчику, который звонит с объекта и читает версию с экрана.
# Снять суффикс — частью того же мержа.
__version__ = "3.0.0-dev"
