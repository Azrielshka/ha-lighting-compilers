# -*- coding: utf-8 -*-
"""Launcher: GUI-обёртка над CLI-скриптами проекта."""

# Версия, которую лаунчер показывает в шапке.
#
# ⚠ Обязана совпадать с тегом, по которому собран EXE: наладчик звонит с объекта
# и читает её С ЭКРАНА. CI сверяет их и падает при расхождении
# (.github/workflows/release.yml), но менять здесь — всё равно вручную.
#
# Порядок: правим здесь -> коммит -> тег vX.Y.Z -> GitHub Actions соберёт EXE.
__version__ = "3.0.0"
