# Оконная библиотека лаунчера.
#
# Нужна двоим:
#   - тому, кто запускает лаунчер ИЗ ИСХОДНИКОВ (python launcher/main.py);
#   - сборке EXE: PyInstaller упаковывает PySide6 внутрь launcher.exe.
#
# Пользователю релизного архива НЕ нужна: у него готовый EXE.
# Отдельным файлом именно поэтому — чтобы не уезжала в requirements.txt.
#
# ⚠ На Linux PySide6 ставится, но не импортируется без libEGL: лаунчер не
# запустится, GUI-тесты сами себя пропустят. На Windows такого нет.

PySide6>=6.8
