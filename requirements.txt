pandas>=2.0
pyarrow>=15.0
openpyxl>=3.1
PyYAML>=6.0

# Деплой на Home Assistant
paramiko>=3.4      # SFTP: файлы в /config через add-on «Advanced SSH & Web Terminal»
websockets>=12.0   # WebSocket API: пространства и этажи (реестры HA)
# GUI launcher
PySide6>=6.8

# EXE build
pyinstaller>=6.0