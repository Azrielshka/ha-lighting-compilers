# Рантайм: то, без чего не работают scripts/*.py.
#
# Этот файл едет в релизный архив — его ставит инженер на объекте.
# Держите его минимальным: каждая строка здесь превращается в мегабайты,
# которые качают на каждой новой машине.
#
# ⚠ PySide6 тут НЕТ намеренно. Ни один скрипт его не импортирует: оконная
# библиотека нужна только исходникам launcher/, а в архив едет собранный
# launcher.exe, внутри которого PySide6 уже упакован PyInstaller'ом. Держали
# его здесь до 2026-07-20 — пользователь качал 0.62 ГБ Qt, чтобы положить рядом
# с EXE ровно то, что в EXE уже есть.
#
# ⚠ pyinstaller тут тоже НЕТ: он собирает EXE, в рантайме бесполезен.

# Чтение таблицы и нормализованный слой
pandas>=2.0
pyarrow>=15.0
openpyxl>=3.1     # ⚠ в коде не импортируется: pandas зовёт его сам на .xlsx
PyYAML>=6.0

# Деплой на Home Assistant
paramiko>=3.4      # SFTP: файлы в /config через add-on «Advanced SSH & Web Terminal»
websockets>=12.0   # WebSocket API: пространства, этажи, карточки
