# ROADMAP — статус проекта

Обновлено: 2026-07-28. Ветка `main` (одна; `feat/table-v2` слита и удалена).
Последний релиз: **v3.2.0** (2026-07-28) — интеграция с Оркестратором здания
(гейт, разметка Areas, бейджи режима — приняты на объекте) и генератор
`zone_manager.json` (шаг 10; live-приёмка на объекте продолжается). Базовые
v3.0.0 / v3.1.0 проверены на чистой машине и на живом объекте. После релиза —
крупный оверхол карточек Главной и этажей (группы этажа по типу, поэтажный
фильтр, новая раскладка; в тег не вошёл, ждёт приёмки).

**Проект в рабочем состоянии.** Последний долг, блокировавший свет на объекте —
JSON Zone Manager — закрыт генератором. Оставшееся — отложенное владельцем
улучшение (заморозка UI).

Генератор конфигурации Home Assistant для DALI-освещения: одна проектная
Excel-таблица → группы света, пространства, помощники, скрипты, автоматизации
и карточки Lovelace. Работает на машине наладчика, опционально деплоит на
объект.

Переезд на таблицу v3 завершён и влит в `main` (`9c0fffd`).

**Почему всё устроено именно так — `docs/internal/decisions.md`.** Этот файл
отвечает только на вопрос «что сделано и что осталось».

## Документация

| Файл | О чём |
|---|---|
| `docs/internal/decisions.md` | **почему так**: разбор решений, из чего выбирали, капканы |
| `docs/internal/data_model.md` | правила модели: привязка устройств, `None`, адреса, канон нейминга, единицы обслуживания |
| `docs/internal/parquet_reference.md` | что лежит в каждой колонке parquet, с примерами |
| `docs/internal/external-interface.md` | интерфейс Оркестратора: реальные `entity_id`, форма гейта, контракт §9 (от автора Оркестратора) |
| `docs/internal/contract-*.md` | архив переписки с автором Оркестратора: требования, разбор, ответ |
| `scripts/_lib/schemas.py` | машиночитаемая схема; при расхождении с документами верна она |

---

# Принципы, которые не меняются

1. Excel читается один раз, дальше — только parquet.
2. Правила нейминга живут в `scripts/_lib/canon.py`, генераторы их не дублируют.
3. YAML генерируется текстом, а не сериализацией: наладчик читает результат
   глазами и сверяет со своей таблицей.
4. **Каждый шаг запускается отдельно, из CLI, без GUI и без предыдущего шага.**
5. **`Build All` офлайновый.** В живую систему пишет только деплой, по явному
   действию.
6. Launcher — orchestration layer без бизнес-логики.
7. Целевая машина — Windows наладчика. Это не интеграция HA, а внешний
   генератор конфигураций с опциональным деплоем.

---

# Пайплайн (10 шагов, работает)

```
Excel
  │
  ├─  1. validate_excel.py          проверка таблицы; на ошибках пайплайн стоит
  ├─  2. normalize_excel.py         → devices / groups / spaces / units / neighbors .parquet
  │
  ├─  3. generate_lights_groups.py  → lights_group.yaml
  ├─  4. generate_general_groups.py → lights_general_groups.yaml
  ├─  5. generate_floor_groups.py   → lights_floor_groups.yaml
  ├─  6. generate_areas.py          → areas.yaml            (Areas + Floors)
  ├─  7. generate_helpers.py        → lighting-compilers.yaml (помощники)
  ├─  8. generate_scripts.py        → scripts.yaml          (клоны шаблонов)
  ├─  9. generate_automations.py    → automations.yaml + blueprints/
  └─ 10. generate_zone_manager.py   → zone_manager.json     (зоны датчиков)
```

Не в пайплайне: `generate_lovelace_cards.py` (карточки → views дашборда),
`show_normalized.py` (просмотр parquet), `check_sftp.py` (разведка канала
деплоя), `backup_dashboard.py` (снимок дашборда перед деплоем карточек).

---

# Осталось

## Отложено владельцем

- [ ] **UI блокируется на время выполнения.** `subprocess.run()` держит поток,
      окно не отвечает: Build All на тестовой таблице — 8.7 с, на объекте в
      76 помещений порядка 12–15 с, на 12 этажей до полуминуты. Windows
      показывает «Не отвечает».

      ⚠ Замер вскрыл вторую сторону: почти всё это время — **не работа, а
      холодный старт Python**. Каждый из десяти шагов тратит ~950 мс на импорт
      интерпретатора и pandas, сама генерация занимает миллисекунды. Лечится
      двумя независимыми способами: вынести выполнение из GUI-потока
      (QThread/QProcess — окно перестанет висеть) и прогонять пайплайн одним
      процессом (уберёт ~8 с из 8.7). Второе выгоднее: оно убирает ожидание,
      а не показывает его красиво.

      ⚠ Есть и вторая, менее очевидная сторона — она была записана в
      `know-limitation.md` ещё в марте и с тех пор не изменилась: кнопки
      выглядят выключенными, но **клики по ним копятся в очереди Qt** и
      срабатывают, когда синхронный шаг закончится. То есть наладчик, ткнув
      «Build All» дважды на подвисшем окне, получит второй прогон. Лечится тем
      же переносом в отдельный поток.

      Отложено 2026-07-18.

- [x] **JSON Zone Manager** — закрыт генератором (2026-07-23, шаг 10). См.
      «Закрытые долги» ниже.

## Релиз

- [x] CHANGELOG: снят `Unreleased`, проставлена дата 2026-07-20
- [x] тег `v3.0.0` поставлен на `3b3b840`, релиз опубликован (2026-07-20).
      Нумерация синхронизирована: теги шли `v1.0.x`, CHANGELOG дошёл до `2.1.0`.

### Что подтвердил живой релиз

Владелец скачал архив, поставил на чистую машину и прошёл весь путь:

- [x] **EXE самодостаточен** — открывается на машине, где стоит только
      `requirements.txt` (без PySide6). Это был главный открытый вопрос
      разведения зависимостей: проверить его из репозитория нельзя, PyInstaller
      собирает под свою ОС. Косвенно подтвердилось ещё в логе сборки — анализ
      тянул только хуки PySide6, pandas в EXE не попал.
- [x] **331 МБ рантайма достаточно** — весь пайплайн и деплой работают.
- [x] **Все 9 шагов пайплайна** отработали из релизной папки.
- [x] **Деплой на объект работает.**

Две сборки упали до этого — обе по моим огрехам в коде, который нельзя прогнать
локально (кодировка вывода в CI, `REM` вместо `#` в PowerShell-шаге). Оба класса
теперь закрыты тестами: `tests/test_output_encoding.py`,
`tests/test_release_workflow.py`.

⚠ **Тег публикует.** `git push origin v3.0.0` запускает сборку и создаёт
**публичный** GitHub Release с EXE. Репозиторий открытый — релиз увидят все.
Перед тегом прогоните `pytest` руками: CI их не гоняет (решение владельца).

## Открыто

- [ ] **Сравнение двух фильтров навигации.** На этажных страницах сейчас
      работают оба: семь плиток (мультивыбор) и выпадающий список (однозначный).
      Владелец сравнивает на объекте, какой удобнее.

      ⚠ Убирая проигравший, снимите И его условие видимости в
      `build_compact_card`. Уберёте только помощника — карточки останутся
      привязаны к несуществующей сущности, и этаж окажется пустым. Пометки
      «ВРЕМЕННОЕ» стоят в четырёх местах: канон, `generate_helpers`,
      `nav_filter.yaml`, `build_compact_card`.

## Оркестратор здания — сделано, ждёт приёмки и объекта (2026-07-22)

Договор — `docs/internal/contract-*.md` (три файла: требования, наш разбор,
ответ автора). Решения — `decisions.md`, раздел «Оркестратор здания».

- [x] **Правка 1. Гейт датчиков** в шести blueprint'ах, обе роли. Fail-open
      шаблоном `states(...) != 'off'` — `condition: state` роняет автоматизацию
      в fail-closed (проверено по исходникам HA 2026.5.4, согласовано с автором).
- [x] **Правки 2–4. Разметка Areas:** одна световая сущность на Area, метка
      `ba_floor_area` на этажных, `ba_type_*` на комнатных. Клиент WebSocket
      расширен (`label_registry`, `entity_registry/update`, `area_registry/update`).
- [x] Тех.помещения без Area; метку opt-out автор отозвал.

- [x] **Приёмка меток и света на объекте пройдена (2026-07-23).** Проверено
      шаблоном в Developer Tools: у комнатных Areas метка `ba_type_*` и один
      `light.*_obshchii`, у этажных — `ba_floor_area` и `light.ves_<N>_i_etazh`,
      света ровно один на Area. Сетевые команды реестра тем самым подтверждены
      на живом HA.

- [x] **Имя гейта исправлено на реальное (2026-07-23).** `external-interface.md`
      §1 вскрыл: контрактная формула `building_automation_sensors_allowed_floor_<N>`
      на объекте не существует — гейт молча указывал в пустоту, fail-open это
      маскировал. Актуальное имя `binary_sensor.orkestrator_zdaniia_datchiki_razresheny_etazh_<N>_etazh`
      зашито в `canon.ba_gate_entity`, тест-якорь `test_gate_entity_id_matches_object`.

- [x] **Гейт на объекте принят (2026-07-23).** Оркестратор управляет гейтом,
      датчики следуют режиму этажа. Fail-open проверен: без гейта работают как
      раньше.

- [x] **Правка 5 выполнена (2026-07-23).** Генерация `input_boolean.regim_auto_<N>`
      закомментирована (не удалена). Бейджи переключены на сущности Оркестратора:
      этаж — `switch...avtomatika_etazh_<N>` (кликабельный тумблер режима) +
      `binary_sensor...datchiki_razresheny_etazh_<N>` (гейт-индикатор датчиков);
      Главная — бейдж-select `select...rezhim_upravleniia_zdaniem` (режим здания).

      ⚠ На существующих объектах осиротевшие `input_boolean.regim_auto_<N>`
      нужно убрать вручную — генератор их больше не создаёт, но и не сносит.

- [x] **Бейджи режима приняты на объекте (2026-07-23).** switch этажа
      переключает Авто/Ручной с бейджа, select здания даёт выбор режима,
      гейт-индикатор показывает состояние датчиков.

**Интеграция с Оркестратором завершена.** Все правки контракта (гейт, разметка
Areas, бейджи) сделаны и приняты на объекте.

## Переработка карточек — сделано, ждёт приёмки на объекте (2026-07-28)

Крупный оверхол дашборда после v3.2.0 (в тег не вошёл). Подробности —
`decisions.md`, раздел «Переработка карточек Главной и этажей».

- [x] **Группы этажа по типу** `light.<этаж>_<тип>` (generate_floor_groups) +
      поэтажный фильтр `input_select.floor_type_filter_<N>` с conditional-плитками.
- [x] **Главная**: тех.плитка → switch автоматики (свет и switch разными
      строками, inline); секции ×3, карточки columns 9; сервис +2 заглушки
      (problemy/monitoring); бейджи center/bottom/scroll; нумерация навигации по
      возрастанию.
- [x] **Этажи**: бейджи center/bottom/scroll; карточка помещения inline +
      grid_options; панель фильтра (заголовок + 7 плиток + список nav_type_pick);
      секции — мультивыбор 1, помещения 2.

- [ ] **Приёмка на объекте.** Проверено текстом YAML и тестами; `layout: center`,
      `features_position: inline` и поведение conditional-фильтра подтвердит живой
      HA. Требует деплоя (новые группы, helpers, карточки).

## Проверка на объекте — пройдена (2026-07-20)

- [x] Карточки помещений и этажей
- [x] **Главная и секция «Сервис»** — после всех правок: плитка тех.помещений
      только там, где группа есть; посев сервисных страниц; выравнивание кнопок

Вся конфигурация, которую собирает пайплайн, проверена на живом объекте.

---

# Закрытые долги «система собрана, но не заработает»

**JSON Zone Manager закрыт генератором** (2026-07-23). Раньше собирался вручную
и блокировал свет: без него `zone_manager.get_sensor_config` возвращал
`found: false` и свет по датчикам не включался. Теперь в таблице заполнен лист
«Группы соседей», а шаг 10 (`generate_zone_manager.py`) собирает
`zone_manager.json` из него — как остальные артефакты. Деплой кладёт файл в
`/config/zone_manager.json`. Осталась приёмка на объекте (см. раздел
«Zone Manager» ниже). Правила сборки — `docs/internal/plan-zone-manager.md`.

**`input_number.vacant_delay` закрыт** (2026-07-17, переработан 2026-07-23): его
создаёт `generate_helpers.py`. С 2026-07-23 — по одному на тип помещения, без
`initial` (значение принадлежит объекту). Долг «свет не гаснет» закрыт.

**Не чиним по решению владельца:** коллизия `room_slug` — два разных названия
помещений могут дать один slug (`201_Зал` и `201_зал` → `201_zal`), и в HA
появятся две группы с одинаковым `unique_id`.

## Zone Manager — сделано, ждёт приёмки (2026-07-23)

- [x] Лист «Группы соседей» читается нормализатором → `neighbors.parquet`
      (forward-fill, позиционные тройки, заглушки, различение пусто/маркер).
- [x] `generate_zone_manager.py` → `data/json/zone_manager.json` (правило группы
      света, `NAME SPACE`, `version`, группировка по помещению).
- [x] Валидация: пустая ячейка → предупреждение, `-`/None → заглушка, битые
      ссылки → предупреждение.
- [x] Деплой цель `zone_manager` → `/config/zone_manager.json`; напоминание
      вызвать `zone_manager.reload` (деплой сервисы не трогает).

- [ ] **Приёмка на объекте.** Заполнить лист «Группы соседей» целиком (сейчас в
      фикстуре 13 зон), сгенерировать, задеплоить, вызвать reload, проверить, что
      `get_sensor_config` находит зоны и свет по датчикам включается.
- [ ] Заменить в листе дубли-«дальние» на явный `-` там, где дальнего нет
      (рантайм тот же, но JSON станет чище — см. plan-zone-manager.md).

---

# Открытые вопросы

| # | Вопрос | Блокирует | Статус |
|---|---|---|---|
| 1 | Монолитные шаблоны карточек или сборка из блоков | Lovelace | ✅ сборка из блоков, обёртка на тип |
| 2 | Как подставлять датчики, когда их число ≠ числу зон | Lovelace | ✅ 0 → пусто, >1 → стопкой |
| 3 | Включать ли `sftp: true` в SSH-аддоне (иначе только SCP) | Живой транспорт | ✅ да, включаем |
| 4 | Подписи расположения в классе (у двери/у окна) | Lovelace | заглушка `—`, владелец правит руками |
