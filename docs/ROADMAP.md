# ROADMAP — переезд на таблицу v3

Обновлено: 2026-07-14. Ветка `feat/table-v2`.

Цель: адаптировать пайплайн под новый формат таблицы и добавить генераторы
скриптов, автоматизаций, пространств и шаг деплоя на Home Assistant.

## Документация

| Файл | О чём |
|---|---|
| `docs/internal/data_model.md` | правила модели: привязка устройств, `None`, адреса, канон нейминга, единицы обслуживания |
| `docs/internal/parquet_reference.md` | что лежит в каждой колонке parquet, с примерами |
| `scripts/_lib/schemas.py` | машиночитаемая схема; при расхождении с документами верна она |

---

# Принципы, которые не меняются

1. Excel читается один раз, дальше — только parquet.
2. Правила нейминга живут в `scripts/_lib/canon.py`, генераторы их не дублируют.
3. YAML генерируется текстом, а не сериализацией: наладчик читает результат
   глазами и сверяет со своей таблицей.
4. **Каждый шаг запускается отдельно, из CLI, без GUI и без предыдущего шага.**
5. **`Build All` офлайновый.** В живую систему пишет только деплой, по явному
   действию.
6. Launcher — orchestration layer без бизнес-логики.
7. Целевая машина — Windows наладчика. Это не интеграция HA, а внешний
   генератор конфигураций с опциональным деплоем.

---

# Пайплайн (8 шагов, работает)

```
Excel
  │
  ├─ 1. validate_excel.py          проверка таблицы; на ошибках пайплайн стоит
  ├─ 2. normalize_excel.py         → devices / groups / spaces / units .parquet
  │
  ├─ 3. generate_lights_groups.py  → lights_group.yaml
  ├─ 4. generate_general_groups.py → lights_general_groups.yaml
  ├─ 5. generate_floor_groups.py   → lights_floor_groups.yaml
  ├─ 6. generate_areas.py          → areas.yaml            (Areas + Floors)
  ├─ 7. generate_scripts.py        → scripts.yaml          (клоны шаблонов)
  └─ 8. generate_automations.py    → automations.yaml + blueprints/
```

Не в пайплайне: `show_normalized.py` (просмотр parquet),
`check_file_editor.py` (разведка транспорта деплоя).

---

# Сделано

## Этап 0–1 — окружение, канон, схема ✅

Dev-окружение на Linux VM, `requirements-dev.txt`, pytest.
`canon.py`: `parse_addr()`, билдеры сущностей, `normalize_space_type()`,
`NONE_TOKENS`. `excel_schema.py`: лист по имени, колонки v3.

## Этап 2 — `validate_excel.py` ✅

Первый шаг. Ничего не пишет в `normalized/`, печатает отчёт и
`data/validation_report.json`. Коды возврата: `0` — можно генерировать,
`1` — блокирующие ошибки (или предупреждения при `--strict`), `2` — файл не найден.

**Блокирующие ошибки:**

| Код | Правило |
|---|---|
| `E01` | нет листа «Проектная БД» |
| `E02` | нет обязательной колонки |
| `E03` | адрес лампы не в формате `X.Y.Z` (или `None` в колонке «Лампа») |
| `E04` | дубликат адреса лампы |
| `E05` | дубликат адреса датчика |
| `E06` | дубликат адреса панели |
| `E07` | один датчик привязан к двум группам (поверх `E05`) |
| `E08` | группа без ламп |
| `E09` | помещение без ламп / без групп |
| `E10` | в помещении колонка «Датчик» пуста во всех строках (нужен адрес или `None`) |
| `E11` | то же для «Панель» |
| `E12` | устройство в строке без группы |
| `E13` | `group_id` встречается в двух помещениях |
| `E14` | нераспознанное значение в «Датчик»/«Панель» |
| `E15` | блок смешивает разные семейства |
| `E16` | блок задан у помещения, которое не автоматизируется |

**Предупреждения:**

| Код | Правило |
|---|---|
| `W01` | помещение без типа → исключается из карточек |
| `W02` | неизвестный тип помещения |
| `W03` | этаж в адресе ≠ колонка «Этаж» (истина — в адресе) |
| `W04` | шина в адресе ≠ колонка «Шина DALI» |
| `W05` | группа без датчиков (кроме `zal`) |
| `W06` | пустая строка внутри листа |
| `W07` | в единице обслуживания больше 12 датчиков |
| `W08` | автоматизируемое помещение без датчиков |
| `W09` | префикс группы ≠ номер помещения (описка при копировании строки) |

## Этап 3 — `normalize_excel.py` ✅

Длинный формат: **строка = одно устройство**. `device_rows.parquet` упразднён.

**Решение:** normalize отказывается работать на таблице с блокирующими ошибками
(зовёт ту же `validate()`), обход — `--force`. Иначе пайплайн не падает, а тихо
врёт — ровно та проблема, из-за которой затевался переезд.

### 3.1 Жёсткая схема parquet ✅

`pa.Table.from_pandas()` выводил типы из данных: на объекте без панелей
`panels_by_group` становился `list<list<null>>`. Схема файла зависела от объекта,
и генератор сломался бы не у разработчика, а у наладчика. Теперь схема
объявлена явно в `schemas.py`, запись идёт строго по ней.

### 3.2 `show_normalized.py` ✅

Parquet бинарный, глазами не открыть. Режимы: обзор, `--space`, `--group`, `--full`.

## Этап 4 — группы света ✅

Три генератора на новую схему. Фильтры переехали из констант в исходнике в CLI
(`_lib/filters.py`): раньше, чтобы исключить этаж, наладчику пришлось бы править
код, а лаунчер передать их не мог.

**Исправлен дефект v1: висячие сущности.** У `generate_general_groups.py` стояло
`EXCLUDE_SPACE_CONTAINS = ["koridor"]` — артефакт отладки перед ПНР. Коридоры не
получали общую группу, но `generate_floor_groups.py` всё равно тянул их
`general_light_entity` в группу этажа: в HA появлялась группа со ссылкой на
несуществующую сущность.

`TECHNICAL_SPACE_TYPES = {korridor, special, recreation}` — проходные
пространства. `tex_floor_<n>` создаются по умолчанию, отключаются
`--no-tech-groups`.

## Этап 5 — Areas и Floors ✅

Основано на `Auto-area-HA`, который уже читал наш `spaces.parquet`. Разрезан
надвое: `transform.py` → наш генератор, `ha_client.py` → транспорт деплоя.

**Выброшен `TRANSLATION_DICT`:** он восстанавливал русское имя из транслита по
словарю (`103_vestibiul` → `103_Vestibiul`, латиница). Костыль под таблицу v1,
где `space` выглядел как `403_кабинет_медиц`. В v3 колонка «Название помещения»
уже человеческая — имя Area берём прямо из неё, транслит уходит в `aliases`.

Шаг офлайновый: тест `test_generator_makes_no_network_calls` это стережёт.

## Этап 6 — единицы обслуживания ✅

Основание: `uncledrew/logic-contol-system/` — схема
`Датчик → Blueprint → Zone Manager → Скрипт`. Blueprint'ы и скрипты статичны;
генерировать надо клоны скриптов и автоматизации под них.

**Колонка «Блок».** Единица обслуживания = помещение (если блок пуст) либо все
помещения одного блока. Правила заказчика («лестницы по стояку», «санузлы с
ближайшими») опираются на пространственную информацию, которой в таблице нет:
эвристика работала бы на одном объекте и молча врала на другом. Решение
принимает проектировщик, глядя на план.

Семейства: `korridor`/`recreation` → `default`, `hall` → `hall`,
`special` → `special`, `class`/`zal` → не автоматизируются.

## Этап 7 — скрипты ✅

`generate_scripts.py` клонирует шаблоны из `templates/scripts/` по единицам.

**Зачем клонировать:** один экземпляр скрипта в HA — это одна очередь. При
тысяче датчиков вызовы копятся, и свет отстаёт от человека; `mode: queued` не
спасает. У каждой единицы свой набор — они друг друга не ждут.

Меняются ровно две вещи: корневой ключ (из него HA делает entity_id) и `alias`
(иначе в UI было бы двести одинаковых «Шаблон скрипта»). Тело не трогаем —
имён в нём нет, конфигурацию скрипт получает параметрами.

## Этап 8 — автоматизации ✅

`generate_automations.py` — экземпляры blueprint'ов, по две на единицу (ON и OFF).
Плюс копии blueprint'ов в `data/blueprints/` для деплоя.

**Пакет, а не `automations.yaml`:** туда HA пишет автоматизации, созданные через
UI, и перезапись стёрла бы ручную работу наладчика.

**У `special` OFF вход один** (`off_script`), у `default` и `hall` — два.
Имена входов описаны явно в `BLUEPRINT_INPUTS_BY_FAMILY` и сверяются тестом с
самими blueprint'ами: опечатка не уронила бы генерацию — HA молча
проигнорировал бы вход, и автоматизация осталась бы без скрипта.

## Launcher ✅ (частично)

- кнопка `Validate Excel` — первая, она же шаг 1 в `Build All`
- `Build All` = 8 шагов, останавливается на ненулевом коде возврата
- галочка `Strict`, кнопка «Открыть папку с результатами»
- `Lovelace` вне `Build All` (карточки вставляются в дашборд вручную)
- фильтры в GUI намеренно не выведены (владелец): нужны редко и при отладке

---

# Осталось

## Карточки Lovelace — сделано (2026-07-15)

`generate_lovelace_cards.py` (v3): у каждого `space_type` **своя раскладка**
(согласовано с владельцем). Собирается методом «обёртка + блоки»: обёртка типа
(`templates/lovelace/<type>/wrapper.yaml`) с маркерами, атомарные плитки
(`_blocks/`), генератор разворачивает повторяющиеся части по правилу типа.

| Тип | Раскладка | Источник |
|---|---|---|
| korridor | пары `свет\|датчик`, балансная нарезка по 3 зоны, макс. 2 сетки в ряд с переносом | данные |
| hall | = korridor | данные |
| special | вертикальный список пар `свет\|датчик` | данные |
| class | грид N колонок: ряд света / ряд датчиков / ряд подписей (заглушка `—`) | данные + подпись руками |
| zal | группы света + **захардкоженные** пресеты-сценарии | данные + хардкод, mushroom/card_mod |
| recreation | общий свет + узкие группы (mushroom) + датчики в 2 колонки | данные, mushroom |

- «датчиков ≠ зон» решено структурно: 0 → пустая ячейка, >1 → стопкой.
- Помещение без валидного типа карточку не получает (`has_valid_type`, W01).
- Кнопка в лаунчере есть, **в Build All не входит**.
- Требование объекта: для zal/recreation на HA должны стоять HACS-карты
  mushroom + card_mod.
- Тесты: `tests/test_generate_lovelace_cards.py` (нарезка, перенос 7–9 зон,
  сборка всех типов, валидность YAML).

### Views дашборда и деплой карточек (2026-07-16)

Экспорт в `.txt` с копипастом руками (наследие v1) **упразднён**. Карточки
раскладываются по views дашборда и деплоятся, как остальные артефакты.

| Что | Куда |
|---|---|
| view на этаж (`zm-floor-<N>`) | компактные карточки помещений этажа |
| subview на пространство (`zm-space-<room_slug>`) | полная карточка, `subview: true` |

- **Компактная карточка** (`_blocks/compact_card.yaml`) — заголовок + общий свет
  + кнопка «Подробнее» с `tap_action: navigate` в subview. Без неё subview был бы
  недостижим: он скрыт из вкладок по определению.
- Оба вида views — `type: sections`: наши карточки используют `grid_options`,
  вне sections-view они не разложатся.
- **Выход:** файл на view в `data/lovelace/` (`--dashboard` задаёт url_path,
  из него строятся navigate-пути; на каждом объекте свой).
- **Деплой** — цель `lovelace`, канал **WebSocket** (не файлы в `/config`):
  `lovelace/config` (прочитать) → `ha_views.merge_views` → `lovelace/config/save`.
  Дашборд остаётся **storage/UI-режимным**, редактируется руками как прежде.
- **Слияние:** свои views опознаются по префиксу пути `zm-` (аналог `zm_` у
  файлов). Чужие views (Главная, Энергомониторинг, Ошибки) не трогаются;
  наши встают на фиксированную позицию — сразу после первого view, чтобы
  регенерация не уносила этажи в хвост. Идемпотентно.
- **Рестарт не нужен** — конфиг дашборда применяется сразу.
- ⚠ `lovelace/config/save` требует токен **администратора** и перезаписывает
  дашборд целиком — поэтому логика слияния вынесена в чистый
  `_lib/ha_views.py` и покрыта `tests/test_ha_views.py` (идемпотентность,
  сохранность чужих views, порядок, сводка для dry-run).

## Деплой

**Одна кнопка `Deploy` с галочками:** что отправлять. Сеть трогается только здесь.

Транспорт выяснен:

| Что | Куда | Чем |
|---|---|---|
| Areas, Floors | реестры HA | **WebSocket API** (обкатан в `Auto-area-HA`) |
| Views карточек | конфиг дашборда | **WebSocket API** (`lovelace/config/save`, нужен админ-токен) |
| YAML-файлы | `/config` | **SCP через Advanced SSH add-on** |

**File Editor не годится:** прокси `/api/hassio/` в HA работает по белому
списку, `ingress/session` и `addons/*/info` в него не входят → 401 при любом
токене (проверено `check_file_editor.py` на живом HA).

Особенности SSH-транспорта (`uncledrew/ssh_connection_cooldrew.md`):
SFTP не работает → нужен `scp -O`; обязательны `Ciphers=aes256-ctr` и
`MACs=hmac-sha2-256-etm@openssh.com`, иначе `Corrupted MAC on input`;
порт и юзер свои на каждом объекте → поля в лаунчере, не константы.

### Раскладка на HA (согласована 2026-07-14)

```yaml
# configuration.yaml — у владельца уже так, менять НЕ надо
homeassistant:
  packages: !include_dir_merge_named includes/packages/

automation manual: !include_dir_merge_list  includes/automations/
script manual:     !include_dir_merge_named includes/scripts/
```

| Наш артефакт | Куда на HA | Формат файла |
|---|---|---|
| 3 файла групп света | `includes/packages/zm_*.yaml` | пакет (маппинг) |
| `scripts.yaml` | `includes/scripts/zm_scripts.yaml` | **словарь** `object_id → скрипт` |
| `automations.yaml` | `includes/automations/zm_automations.yaml` | **голый список** |
| 6 blueprint'ов | `blueprints/automation/zone_manager/` | как есть |
| Areas, Floors | реестры HA | WebSocket, файла нет |

⚠ Форматы не взаимозаменяемы: `merge_list` ждёт список, `merge_named` — словарь.
Домен `automation:` в HA — список, `script:` — словарь. Перепутать = конфигурация
не загрузится. Тесты `test_file_is_a_bare_list` и `test_scripts_file_is_a_mapping`
это стерегут.

Префикс `zm_` в именах файлов — чтобы деплой перезаписывал только своё и
никогда не трогал файлы наладчика.

### Сделано (2026-07-14)

- [x] `_lib/ha_targets.py` — раскладка «артефакт → путь на HA». Чистая логика,
      покрыта тестами целиком: вынесена отдельно именно затем, чтобы
      проверяться без живого HA
- [x] `scripts/deploy.py` — **dry-run работает полностью**: показывает, что и
      куда поедет, размеры, чего не хватает и какой шаг пайплайна запустить
- [x] галочки `--targets lights,scripts,automations,blueprints,areas,lovelace`
- [x] дифф пространств: идемпотентность по имени (повторный деплой не дублирует)
- [x] Launcher: кнопка `Deploy` + отдельный диалог (поля SSH и HA, галочки,
      подтверждение перед LIVE). **Не в `Build All`**
- [x] URL/токен/SSH сохраняются в конфиг лаунчера (он в `.gitignore`)
- [x] бэкап не делаем (владелец: файлов на HA не предполагается)

### Живой транспорт ✅ (2026-07-14)

- [x] `_lib/ha_ssh.py` — SFTP на paramiko, прибитые `aes256-ctr` /
      `hmac-sha2-256-etm@openssh.com`. **Проверено на живом HA**: хендшейк,
      ключ, SFTP, создание вложенных папок, запись со сверкой размера.
      Грабля: в paramiko это `digests`, а не `macs`
- [x] `_lib/ha_ws.py` — WebSocket (Areas + Floors) на базе `Auto-area-HA`.
      Привязка Area→Floor, идемпотентность по имени, TLS-проверка включена по
      умолчанию (`--insecure` для самоподписанного https).
      **Сеть проверяется на объекте**: снаружи HA только через Traefik с
      самоподписанным сертификатом, а наладчик ходит локально
- [x] `check_sftp.py` — разведка канала для наладчика на новом объекте
- [x] `--live` реально заливает файлы; частичный отказ — доливаем остальные
- [x] диалог лаунчера: галочки целей, поля SSH/HA, `--insecure`, подтверждение

Проверено на живом объекте (192.168.8.49, 2026-07-14):
- [x] SFTP — все файлы задеплоились в /config
- [x] WebSocket — 1 этаж + 6 пространств созданы с первого раза;
      имена полей реестров (level, icon, floor_id) угаданы верно
- [x] идемпотентность — повторный LIVE пропустил всё существующее, без дублей

**Рестарт HA деплой не делает и делать не будет** (решение владельца). Без него
изменения не применятся: `light: - platform: group` — YAML-платформа, у неё нет
сервиса reload. Деплой напоминает об этом крупно.

## Launcher

- [ ] кнопка `Deploy` — с подтверждением, не в `Build All`
- [ ] UI блокируется на время выполнения (QThread). На объекте в 12 этажей
      окно будет выглядеть зависшим

## Сопровождение

- [x] README под v3
- [x] `architecture_rules.md` и `project_context.md` — свёрнуты в архивные
      указатели на актуальные доки
- [x] имя проекта приведено к `ha-lighting-compilers` (заголовок окна, пути,
      release-артефакт, doc `data_model_v2.md` → `data_model.md`); удалены
      мёртвые `*_backupstep*.py`
- [ ] мерж в `main` + тег `v3.0.0` (теги идут `v1.0.x`, CHANGELOG — `2.1.0`;
      нумерация синхронизируется на `v3.0.0`)

## Главная страница — генерируем целиком

Решено 2026-07-16. Источник требований — Главная с объекта «Колледж Химки»
(4 этажа, ~76 помещений), которую владелец дал **как образец желаемого**.

⚠ Это **не объект миграции**. Делаем для песочницы и будущих объектов, где
ничего ещё не создано: ни помощников, ни ручных views, ни Areas-этажей.
Поэтому совместимость с химкинскими именами не требуется — конвенции
выбираем свободно, «с чистого листа».

### Что убираем из текущей Главной

- кастомный фон (`background:`);
- карточку «Выбор режима работы колледжа» целиком (`input_select.mode_work_colhim`,
  `input_button.but_save_mode`, `sensor.tekushchii_rezhi_raboty_kolledzha`,
  `switch.dali_auto_brightness_master`);
- виджеты шапки (`badges`: режим, `input_text`, календари).

### Блок на этаж

1. `type: area` с картинкой и переходом на этажный view;
2. `horizontal-stack`: свет этажа + свет тех.помещений;
3. список выбора помещения (`input_select`);
4. **кнопка перехода** на выбранное помещение.

### Навигация: 76 `conditional` → 1 markdown-карточка на этаж

Было: `input_select` + по одной `conditional`-карточке на КАЖДОЕ помещение
(39/13/12/12 = 76 штук), каждая с зашитым именем и путём. Причина не в дурном
вкусе: **`navigation_path` у штатных карточек — статическая строка**, шаблоны
в неё не подставляются, и 76 условий были единственным штатным способом
выбрать нужную строку. Настоящий изъян — ручная синхронизация: опции
помощника, условия и пути жили в трёх местах и обязаны были совпадать
символ в символ.

Решение — **markdown-карточка**: она поддерживает Jinja и ссылки на внутренние
дашборды. Одна карточка на этаж вместо 39, карту «имя → слаг» генерируем мы.
Проверено на живом HA — переход работает. Вид утверждён владельцем: ссылка
`display: block` (кликается вся карточка), текст слева, `›` справа через
`a::after`, оформление под этажную кнопку.

⚠ Стилизуется только словарной формой card_mod с `$`: ссылка лежит внутри
`ha-markdown`, у которого свой shadow-root, и строковый `style: |` до неё
не достаёт.

Рассмотренные и отклонённые:
- `config-template-card` — шаблонит любое поле, 1 карточка на этаж, но
  **несущая** HACS-зависимость (стабильный релиз 2021, работа в бете);
- `browser_mod` — то же, несущая зависимость;
- список `weblink`-строк — нативно и в один клик, но это простыня: владельцу
  важно, чтобы **видна была одна выбранная кнопка**, а не весь список;
- генерировать те же 76 `conditional` — рассинхрон уходит, но конфиг растёт
  как O(помещений): ~480 на 12-этажном объекте.

### Area на этаж ✅ (2026-07-16)

`type: area` умеет показывать **только Area**; карточки для Floor в HA нет.
Генерируем `Весь <N> этаж` → `ves_<N>_etazh` (`canon.floor_area_name` /
`floor_area_id`), идут в конце списка, чтобы не сдвигать порядок помещений.
Деплой подхватил без правок — это обычные Areas с привязкой к этажу.

Areas двух смыслов — не конфликт: комнатные наполняет интеграция по
устройствам, групповые светильники этажа устройств не имеют и попадут только
в Area этажа. Подробности и три похожих имени на этаж — в `data_model.md`.

### Порядок работ

**helpers — предусловие Главной, а не довесок.** Навигация требует
`input_select` со списком помещений этажа, и его опции обязаны быть построены
из того же источника, что и карта в markdown, иначе рассинхрон возвращается —
ровно тот, ради ухода от которого всё и затевалось.

1. ✅ `generate_areas.py` — Area на этаж;
2. **шаг helpers** — в т.ч. `input_select` навигации с опциями ← следующий;
3. шаблон Главной + генерация.

### Открыто

- Имена помощников и Areas-этажей — на наше усмотрение (объект чистый),
  но конвенцию зафиксировать в каноне рядом с остальными, а не по месту.

## Вспомогательные объекты (helpers) — отдельный шаг

Помощники накопились по ходу работы: на них ссылаются автоматизации и карточки,
но **пайплайн не создаёт ни одного**. Наладчик заводит их руками, и забытый
помощник всплывает уже на объекте. Собрать их генерацию одним шагом.

Список закрыт по состоянию на 2026-07-16 — при добавлении новых ссылок
дополнять здесь же.

### Класс А — без них система не работает

| Сущность | Кто ссылается | Что без него |
|---|---|---|
| `input_number.vacant_delay` | **каждая** OFF-автоматизация (`canon.VACANT_DELAY_ENTITY`) | `states()` → `unknown` → `for: seconds` ломается → **свет не гаснет** |

Один на объект. Это и есть главный известный долг (см. ниже).

### Класс Б — нужны только карточкам, смысл задаёт владелец

Мы можем создать сами объекты, но **работать они будут, только если владелец
обвяжет их своими автоматизациями**: генератор кладёт выключатель, а не логику.

| Сущность | Сколько | Кто ссылается |
|---|---|---|
| `input_button.but_back` | один на объект | бейдж «назад» в `floor/view.yaml` |
| `input_boolean.regim_auto_<N>` | по одному на этаж | бейдж режима в `floor/view.yaml` |
| `input_boolean.rezhim_tetra` | один на объект | пресет зала в `zal/wrapper.yaml` |
| `input_boolean.polnaia_iarkost` | один на объект | пресет зала |
| `input_boolean.priglushennoe_osveshchenie` | один на объект | пресет зала |
| `input_boolean.rezhim_meropriiatiia` | один на объект | пресет зала |

### Класс В — навигационный, генерируется целиком вместе с опциями

| Сущность | Сколько | Кто ссылается |
|---|---|---|
| `input_select` навигации по этажу | по одному на этаж | markdown-кнопка перехода на Главной |

Отличается от класса Б принципиально: **его опции — это список помещений
этажа**, и они обязаны совпадать символ в символ с картой «имя → слаг» в
markdown-кнопке. Поэтому опции генерируются из того же источника, что и карта.
Отдать заполнение опций владельцу — значит вернуть тот самый рассинхрон, ради
ухода от которого мы и отказались от 76 `conditional`.

### Как делать — набросок, не решение

- **Канал напрашивается существующий:** домены `input_number:` / `input_boolean:` /
  `input_select:` / `input_button:` работают в пакетах, значит помощники — это
  ещё один `includes/packages/zm_helpers.yaml`, который поедет по SFTP вместе
  с группами света. Новых механик не нужно.
- **Имя из ключа, а не из `name`.** В YAML `entity_id` берётся из ключа
  (`vacant_delay:` → `input_number.vacant_delay`), поэтому предсказуем. Через UI
  он выводится из отображаемого имени — та же ловушка, на которой мы уже
  обожглись с группами этажа. Ещё один довод за YAML-пакет.
- ⚠ **Грабля, если помощник уже существует:** объявим в YAML одноимённый с
  заведённым через UI — HA не сольёт их, а создаст второй объект
  (`input_number.vacant_delay_2`). На чистом объекте вопрос не стоит, но на
  объекте с ручными помощниками их надо удалить до первого деплоя.
- Значения по умолчанию (`vacant_delay` — сколько секунд?) взять неоткуда:
  либо константа в каноне, либо колонка в таблице.
- `regim_auto_<N>` и `input_select` навигации требуют списка этажей — он у нас
  есть; остальные штучные.

---

# ⚠ Известный долг: система собрана, но не заработает

Конфигурация полная, но на объекте свет работать **не будет**, пока не появятся
две вещи. Обе — вне пайплайна по решению владельца.

| Чего нет | Что произойдёт |
|---|---|
| `input_number.vacant_delay` | OFF-триггер: `for: seconds: {{ states(...) }}` вернёт `unknown` → **свет не будет гаснуть** |
| JSON Zone Manager | Автоматизация вызовет `zone_manager.get_sensor_config`, получит `found: false` → **свет не будет включаться** |

`input_number.vacant_delay` — один на объект. Закрывается шагом
**«Вспомогательные объекты (helpers)»** выше: там же собран полный список
помощников, на которые ссылаются автоматизации и карточки.

JSON Zone Manager — собирается вручную; для автоматизации понадобился бы лист
«Группы соседей», который пока пуст. Помощником не является — это отдельная
конфигурация, шагом helpers не закрывается.

**Не чиним по решению владельца:** коллизия `room_slug` — два разных названия
помещений могут дать один slug (`201_Зал` и `201_зал` → `201_zal`), и в HA
появятся две группы с одинаковым `unique_id`.

---

# Открытые вопросы

| # | Вопрос | Блокирует | Статус |
|---|---|---|---|
| 1 | Монолитные шаблоны карточек или сборка из блоков | Lovelace | ✅ сборка из блоков, обёртка на тип |
| 2 | Как подставлять датчики, когда их число ≠ числу зон | Lovelace | ✅ 0 → пусто, >1 → стопкой |
| 3 | Включать ли `sftp: true` в SSH-аддоне (иначе только SCP) | Живой транспорт | ✅ да, включаем |
| 4 | Подписи расположения в классе (у двери/у окна) | Lovelace | заглушка `—`, владелец правит руками |
