# Всё для разработки и сборки: рантайм + GUI + тесты + PyInstaller.
#
# Ставится на машине разработчика и на раннере GitHub Actions.
# Пользователю релизного архива нужен только requirements.txt.

-r requirements.txt
-r requirements-gui.txt

pytest>=8.0
pyinstaller>=6.0    # сборка EXE; в рантайме не нужен
